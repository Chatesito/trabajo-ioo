\documentclass[12pt,a4paper]{article}

% ============================================================================
% PAQUETES
% ============================================================================
\usepackage[utf8]{inputenc}
\usepackage[spanish,es-noshorthands]{babel}
\usepackage{geometry}
\usepackage{graphicx}
\usepackage{float}
\usepackage{hyperref}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, positioning, arrows.meta, fit, backgrounds, shadows.blur, calc}
\usepackage{booktabs}
\usepackage{array}
\usepackage{tabularx}
\usepackage{colortbl}
\usepackage{listings}
\usepackage{enumitem}

% ============================================================================
% CONFIGURACIÓN DE PÁGINA
% ============================================================================
\geometry{left=3cm, right=2.5cm, top=2.5cm, bottom=2.5cm}
\setlength{\headheight}{15pt}

\definecolor{primaryblue}{RGB}{25, 55, 109}
\definecolor{secondaryblue}{RGB}{41, 74, 139}
\definecolor{darkgray}{RGB}{50, 50, 50}
\definecolor{codebg}{RGB}{245, 247, 250}
\definecolor{codecomment}{RGB}{106, 135, 89}
\definecolor{codekeyword}{RGB}{204, 120, 50}
\definecolor{codestring}{RGB}{106, 135, 89}

\hypersetup{colorlinks=true, linkcolor=primaryblue, urlcolor=secondaryblue, pdfborder={0 0 0}}
\emergencystretch=3em

\pagestyle{fancy}
\fancyhf{}
\rhead{Taller Docker --- OO}
\lhead{Ingeniería Orientada a Objetos}
\rfoot{\thepage}

\titleformat{\section}{\normalfont\Large\bfseries\color{primaryblue}}{\thesection}{1em}{}
\titleformat{\subsection}{\normalfont\large\bfseries\color{secondaryblue}}{\thesubsection}{1em}{}

% ============================================================================
% CONFIGURACIÓN DE CÓDIGO
% ============================================================================
\lstdefinestyle{codestyle}{
    backgroundcolor=\color{codebg},
    commentstyle=\color{codecomment},
    keywordstyle=\color{codekeyword}\bfseries,
    stringstyle=\color{codestring},
    basicstyle=\ttfamily\footnotesize,
    breakatwhitespace=false,
    breaklines=true,
    captionpos=b,
    keepspaces=true,
    numbers=left,
    numbersep=5pt,
    showspaces=false,
    showstringspaces=false,
    showtabs=false,
    tabsize=2,
    frame=single,
    rulecolor=\color{darkgray}
}
\lstset{style=codestyle}

% ============================================================================
% INICIO DEL DOCUMENTO
% ============================================================================
\begin{document}

% ============================================================================
% PORTADA
% ============================================================================
\begin{titlepage}

    \begin{tikzpicture}[remember picture, overlay]
        \fill[primaryblue] (current page.north west) rectangle ([xshift=1.5cm]current page.south west);
    \end{tikzpicture}
    \begin{tikzpicture}[remember picture, overlay]
        \fill[secondaryblue] ([xshift=1.5cm]current page.north west) rectangle ([xshift=1.7cm]current page.south west);
    \end{tikzpicture}

    \hspace{2.3cm}
    \begin{minipage}[t]{0.78\textwidth}

        \vspace{2cm}

        {\Large\textcolor{primaryblue}{\textbf{ACTIVIDAD IX}}}

        \vspace{0.2cm}
        {\large\textcolor{darkgray}{Ingeniería Orientada a Objetos}}

        \vspace{2.5cm}

        {\Huge\textcolor{primaryblue}{\textbf{Taller Docker --- OO}}}\\[0.3cm]
        {\Huge\textcolor{primaryblue}{\textbf{Clínica Web en Contenedor}}}

        \vspace{0.8cm}
        {\LARGE\textcolor{darkgray}{CRUD de Pacientes y Citas con Django, PostgreSQL y Podman}}

        \vspace{0.5cm}
        {\large\textcolor{darkgray}{\textit{Universidad Surcolombiana}}}\\[0.15cm]
        {\normalsize\textcolor{darkgray}{Facultad de Ingeniería --- Programa de Ingeniería de Software}}\\[0.15cm]
        {\normalsize\textcolor{darkgray}{Neiva --- Huila}}

        \vspace{2.5cm}

        {\large\textcolor{primaryblue}{\textbf{ESTUDIANTES}}}\\[0.5cm]
        \begin{tabular}{@{}l@{\hspace{0.5cm}}l@{}}
            \textbf{Jorge León Chavarro Alarcón}    & Código: 20231211354 \\[0.2cm]
            \textbf{Juan David Rivera Chaté}        & Código: 20231213382 \\[0.2cm]
            \textbf{Luis Alejandro Restrepo Medina} & Código: 20231211869 \\
        \end{tabular}

        \vspace{1.5cm}

        {\large\textcolor{primaryblue}{\textbf{DOCENTE}}}\\[0.5cm]
        \textbf{Eduardo Martínez Vidal}

        \vfill
        \textcolor{primaryblue}{\rule{0.8\linewidth}{1pt}}
        \vspace{0.3cm}
        {\large\textcolor{darkgray}{\today}}

    \end{minipage}
\end{titlepage}

\newpage
\tableofcontents
\newpage

% ============================================================================
% 1. INTRODUCCIÓN
% ============================================================================
\section{Introducción}

El presente informe documenta el desarrollo de la \textbf{Actividad IX} de la asignatura Ingeniería de Software Orientada a Objetos, cuyo objetivo es construir una aplicación con paradigma orientado a objetos, conectada a una base de datos relacional, empaquetada en un contenedor con persistencia de datos mediante volúmenes, y finalmente publicada en Docker Hub.

Se optó por desarrollar una aplicación web de gestión de pacientes y citas para una clínica, implementada en Python con el framework \textbf{Django 6.0.4}, respaldada por \textbf{PostgreSQL 16} como motor de base de datos. Todo el entorno se orquesta mediante \textbf{Podman} (alternativa libre y compatible con Docker, autorizada por el docente) en lugar de Docker, aprovechando que el equipo de trabajo cuenta con sistemas operativos basados en Linux sin la necesidad de un demonio privilegiado.

\section{Objetivos}

\subsection{Objetivo general}

Desarrollar una aplicación orientada a objetos, en contenedor, con persistencia de datos mediante volúmenes y publicada en Docker Hub.

\subsection{Objetivos específicos}

\begin{itemize}
    \item Implementar un CRUD relacional con al menos dos entidades siguiendo el paradigma orientado a objetos.
    \item Diseñar una arquitectura multi-contenedor con separación de responsabilidades entre aplicación y base de datos.
    \item Garantizar la persistencia de la información mediante el uso de volúmenes nombrados.
    \item Automatizar el arranque completo del sistema mediante \texttt{podman-compose} y un script de inicio.
    \item Publicar la imagen resultante en Docker Hub para su distribución.
\end{itemize}

% ============================================================================
% 3. JUSTIFICACIÓN TÉCNICA
% ============================================================================
\section{Justificación técnica}

\subsection{Uso de Podman como runtime de contenedores}

La consigna de la actividad establece Docker como herramienta principal. El equipo solicitó al docente autorización explícita para utilizar \textbf{Podman} como alternativa, permiso que fue concedido para esta entrega. La tabla siguiente sintetiza las diferencias técnicas relevantes que motivaron la elección.

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.4}
\begin{tabularx}{\textwidth}{|>{\bfseries\raggedright\arraybackslash}p{3.3cm}|>{\raggedright\arraybackslash}X|>{\raggedright\arraybackslash}X|}
\hline
\rowcolor[RGB]{25,55,109}
\textcolor{white}{\textbf{Característica}} & \textcolor{white}{\textbf{Docker}} & \textcolor{white}{\textbf{Podman}} \\
\hline
Arquitectura & Cliente y demonio \texttt{dockerd} como root & Sin demonio; procesos del propio usuario \\
\hline
Modelo de seguridad & Requiere grupo \texttt{docker} (privilegios de root) & \textit{Rootless} por defecto \\
\hline
Compatibilidad & Formato OCI & Formato OCI (compatible con imágenes de Docker) \\
\hline
Orquestación local & \texttt{docker compose} & \texttt{podman-compose} \\
\hline
Registries & Cualquier registry OCI & Cualquier registry OCI, incluido Docker Hub \\
\hline
\end{tabularx}
\caption{Comparación técnica entre Docker y Podman}
\end{table}

Razones concretas de la elección:

\begin{itemize}
    \item \textbf{Compatibilidad bidireccional}: el \texttt{Dockerfile} y el \texttt{compose.yml} son 100\% compatibles con Docker. La imagen publicada puede ejecutarse con \texttt{docker compose up} sin modificar un solo archivo.
    \item \textbf{Modelo \textit{rootless}}: los contenedores se ejecutan como procesos del usuario, sin necesidad de un demonio privilegiado en segundo plano.
    \item \textbf{Instalación simplificada en Arch Linux}: \texttt{podman} y \texttt{podman-compose} están disponibles en los repositorios oficiales y se instalan con un solo comando \texttt{pacman}.
\end{itemize}

\subsection{Entorno de desarrollo}

El proyecto se desarrolló sobre \textbf{Linux nativo} (CachyOS, distribución derivada de Arch Linux), instalado directamente sobre la máquina física. Esto corresponde a la opción de mayor exigencia entre las tres planteadas en la guía del docente (Básica, Media y Alta).

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{imagenes/01-cachyos-uname.png}
\caption{Sistema operativo del entorno de desarrollo: CachyOS (Linux nativo).}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{imagenes/02-podman-version.png}
\caption{Versiones de Podman y \texttt{podman-compose} instaladas en el entorno.}
\end{figure}

% ============================================================================
% 4. DESCRIPCIÓN DE LA APLICACIÓN
% ============================================================================
\section{Descripción de la Aplicación}

\subsection{Dominio}

La aplicación modela la gestión básica de una clínica. Permite registrar pacientes con sus datos de contacto y agendar citas asociadas a cada paciente con fecha, hora y motivo. Se implementó un CRUD completo (crear, listar, editar, eliminar) para ambas entidades.

\subsection{Paradigma orientado a objetos}

Django implementa una variante del patrón \textit{Active Record} en su ORM, con elementos de \textit{Data Mapper} en sus \texttt{QuerySet}: cada tabla de la base de datos se representa como una clase Python que hereda de \texttt{django.db.models.Model} y expone atributos tipados y métodos propios del dominio. A continuación se muestran las dos clases principales.

\begin{lstlisting}[language=Python, caption={clinica/models.py}]
from django.db import models
from django.core.validators import RegexValidator


class Paciente(models.Model):
    nombre = models.CharField(max_length=150)
    cedula = models.CharField(
        max_length=20,
        unique=True,
        validators=[
            RegexValidator(
                regex=r"^\d+$",
                message="La cedula debe contener solo numeros.",
            )
        ],
    )
    telefono = models.CharField(
        max_length=20,
        validators=[
            RegexValidator(
                regex=r"^\+?\d+$",
                message="El telefono debe contener solo numeros.",
            )
        ],
    )
    email = models.EmailField()

    def __str__(self):
        return f"{self.nombre} ({self.cedula})"


class Cita(models.Model):
    paciente = models.ForeignKey(Paciente, on_delete=models.CASCADE)
    fecha = models.DateTimeField()
    motivo = models.TextField()

    def __str__(self):
        return f"{self.paciente.nombre} - {self.fecha:%Y-%m-%d %H:%M}"
\end{lstlisting}

Puntos clave del diseño OO:

\begin{itemize}
    \item Cada entidad es una \textbf{clase} con atributos encapsulados y tipados.
    \item La clase \texttt{Paciente} incorpora \textbf{validadores} reutilizables (\texttt{RegexValidator}) que garantizan la integridad del dato a nivel de clase, no únicamente a nivel de formulario.
    \item La relación \textbf{1 a N} entre \texttt{Paciente} y \texttt{Cita} se expresa mediante una \texttt{ForeignKey} con política de borrado en cascada.
    \item El método mágico \texttt{\_\_str\_\_} ofrece una representación en texto útil para depuración, logs y vistas administrativas.
    \item El CRUD se implementa mediante \textit{Class-Based Views} (\texttt{ListView}, \texttt{CreateView}, \texttt{UpdateView}, \texttt{DeleteView}) que heredan de clases base de Django, reutilizando lógica y promoviendo el principio de inversión de dependencias.
\end{itemize}

\subsection{Stack tecnológico}

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.4}
\begin{tabularx}{\textwidth}{|>{\bfseries}p{4cm}|X|}
\hline
\rowcolor[RGB]{25,55,109}
\textcolor{white}{\textbf{Componente}} & \textcolor{white}{\textbf{Tecnología}} \\
\hline
Lenguaje & Python 3.12 \\
\hline
Framework web & Django 6.0.4 \\
\hline
Servidor de aplicación & Gunicorn 23 \\
\hline
Servidor de estáticos & WhiteNoise 6.9 \\
\hline
Base de datos & PostgreSQL 16 (alpine) \\
\hline
Driver DB & psycopg2-binary 2.9.11 \\
\hline
Gestión de entorno & django-environ 0.13 \\
\hline
Contenedores & Podman 5.8 + podman-compose 1.5 \\
\hline
Registro de imágenes & Docker Hub \\
\hline
\end{tabularx}
\caption{Stack tecnológico del proyecto}
\end{table}

\subsection{Interfaz del CRUD}

A continuación se muestra la interfaz funcional sobre la cual el usuario opera. Las vistas implementan las cuatro operaciones del CRUD para las dos entidades del dominio (\texttt{Paciente} y \texttt{Cita}).

\begin{figure}[H]
\centering
\includegraphics[width=0.78\textwidth]{imagenes/07-listado-pacientes.png}
\caption{Listado de pacientes registrados.}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.78\textwidth]{imagenes/08-crear-paciente.png}
\caption{Formulario de creación de paciente.}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.78\textwidth]{imagenes/09-editar-paciente.png}
\caption{Formulario de edición de paciente.}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.78\textwidth]{imagenes/10-eliminar-paciente.png}
\caption{Confirmación de eliminación de paciente.}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.78\textwidth]{imagenes/11-listado-citas.png}
\caption{Listado de citas asociadas a pacientes (relación 1 a N mediante \texttt{ForeignKey}).}
\end{figure}

% ============================================================================
% 5. ARQUITECTURA DE CONTENEDORES
% ============================================================================
\section{Arquitectura de Contenedores}

La solución se compone de \textbf{dos contenedores} orquestados por \texttt{podman-compose}, un \textbf{volumen nombrado} para la persistencia de la base de datos y una \textbf{red privada} para aislar la comunicación entre servicios.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    font=\small\sffamily,
    >={Latex[length=2.8mm,width=2.2mm]},
    box/.style={
        draw=primaryblue!55,
        line width=0.5pt,
        rounded corners=4pt,
        fill=white,
        align=center,
        minimum width=3.8cm,
        minimum height=1.6cm,
        blur shadow={shadow blur steps=5, shadow yshift=-1.5pt, shadow xshift=0pt, shadow blur radius=4pt, shadow opacity=18},
    },
    browser/.style={
        draw=primaryblue!55,
        line width=0.5pt,
        rounded corners=4pt,
        fill=primaryblue!8,
        align=center,
        minimum width=4.2cm,
        minimum height=1.05cm,
        blur shadow={shadow blur steps=5, shadow yshift=-1.5pt, shadow blur radius=4pt, shadow opacity=18},
    },
    cyl/.style={
        draw=primaryblue!55,
        cylinder,
        shape border rotate=90,
        aspect=0.22,
        fill=primaryblue!7,
        align=center,
        minimum height=1.5cm,
        minimum width=2.6cm,
        font=\small\sffamily,
    },
    arrow/.style={->, line width=0.6pt, draw=primaryblue!75},
    flowlbl/.style={
        font=\scriptsize\sffamily,
        text=darkgray,
        fill=white,
        inner sep=2.5pt,
        rounded corners=1.5pt,
    },
    layerlbl/.style={
        font=\footnotesize\bfseries\sffamily,
        inner sep=3.5pt,
        rounded corners=2pt,
        fill=white,
    },
    layer-host/.style={
        draw=gray!45,
        line width=0.5pt,
        rounded corners=10pt,
        fill=gray!4,
        inner sep=0.65cm,
    },
    layer-podman/.style={
        draw=primaryblue!45,
        line width=0.5pt,
        rounded corners=8pt,
        fill=primaryblue!3,
        inner sep=0.55cm,
    },
    layer-net/.style={
        draw=secondaryblue!55,
        line width=0.4pt,
        dashed,
        rounded corners=6pt,
        fill=secondaryblue!6,
        inner sep=0.42cm,
    },
]

    \node[browser] (browser) {\textbf{Navegador del usuario}};

    \node[box, below=2.2cm of browser, xshift=-2.6cm] (web) {%
        \textbf{\color{primaryblue}clinica\_web}\\[2pt]
        {\footnotesize Django + Gunicorn}\\[1pt]
        {\footnotesize\ttfamily :8000}};

    \node[box, right=1.5cm of web] (db) {%
        \textbf{\color{primaryblue}clinica\_db}\\[2pt]
        {\footnotesize PostgreSQL 16}\\[1pt]
        {\footnotesize\ttfamily :5432 (interno)}};

    \node[cyl, below=1.6cm of db] (vol) {\textbf{pgdata}};

    \draw[arrow] (browser) -- node[flowlbl, pos=0.45] {HTTP \texttt{8000:8000}} (web);
    \draw[arrow] (web) -- node[flowlbl, pos=0.5, align=center] {DNS \texttt{db}\\TCP \texttt{5432}} (db);
    \draw[arrow] (db) -- node[flowlbl, pos=0.5] {\texttt{/var/lib/postgresql/data}} (vol);

    \begin{pgfonlayer}{background}
        \node[layer-net, fit=(web)(db)] (net) {};
        \node[layer-podman, fit=(net)(vol)] (podman) {};
        \node[layer-host, fit=(browser)(podman)] (host) {};
    \end{pgfonlayer}

    \node[layerlbl, color=secondaryblue, anchor=west]
        at ([xshift=10pt, yshift=-2pt]net.north west) {Red privada \texttt{clinica\_net}};
    \node[layerlbl, color=primaryblue, anchor=west]
        at ([xshift=10pt, yshift=-2pt]podman.north west) {Podman runtime (rootless)};
    \node[layerlbl, color=darkgray, anchor=west]
        at ([xshift=10pt, yshift=-2pt]host.north west) {Host (sistema Linux)};

\end{tikzpicture}
\caption{Arquitectura de contenedores del sistema.}
\label{fig:arquitectura}
\end{figure}

El diagrama de la figura~\ref{fig:arquitectura} ilustra los tres niveles de aislamiento del sistema. El navegador del usuario accede al puerto 8000 del host, mapeado al contenedor \texttt{clinica\_web} (Django y Gunicorn). Éste se comunica con \texttt{clinica\_db} (PostgreSQL 16) a través de la red privada \texttt{clinica\_net}, resolviendo el nombre del servicio \texttt{db} mediante la DNS interna de Podman. La base de datos persiste sus archivos en el volumen nombrado \texttt{pgdata}, montado en \texttt{/var/lib/postgresql/data}. \textbf{El puerto 5432 de PostgreSQL no se expone al host}: únicamente el contenedor \texttt{clinica\_web} puede comunicarse con la base de datos.

\subsection{Servicio web}

Construye la imagen \texttt{clinica-ioo:latest} a partir del \texttt{Dockerfile}. Expone el puerto 8000 al host y se conecta a la base de datos a través del nombre de servicio \texttt{db}, resuelto automáticamente por la DNS interna de Podman.

\subsection{Servicio db}

Usa la imagen oficial \texttt{postgres:16-alpine}. \textbf{No} expone su puerto al host por razones de seguridad: únicamente el contenedor \texttt{web} puede comunicarse con la base a través de la red privada \texttt{clinica\_net}. Implementa un \textit{healthcheck} con \texttt{pg\_isready} para que Podman conozca su estado.

\subsection{Persistencia}

El volumen nombrado \texttt{pgdata} se monta en \texttt{/var/lib/postgresql/data}. Esto garantiza que la información persista entre reinicios, reconstrucciones de la imagen del servicio web, y apagados del equipo. El volumen únicamente se elimina con la opción explícita \texttt{podman-compose down -v}.

\subsection{Verificación de la red privada}

La red \texttt{clinica\_net} aísla la comunicación entre los contenedores y permite la resolución DNS interna por nombre de servicio. Al inspeccionar la red se observan los dos contenedores conectados y el rango de direcciones asignado por Podman.

\textit{Nota sobre el nombre real:} \texttt{podman-compose} antepone el nombre del proyecto (la carpeta donde reside el \texttt{compose.yml}) al nombre lógico de la red declarada. Por ese motivo, la red declarada como \texttt{clinica\_net} aparece en el sistema como \texttt{trabajo-ioo\_clinica\_net}, tal como se observa en la figura siguiente.

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{imagenes/06-network-inspect.png}
\caption{Salida de \texttt{podman network inspect} sobre la red del proyecto.}
\end{figure}

% ============================================================================
% 4. ARCHIVOS CLAVE DEL PROYECTO
% ============================================================================
\section{Archivos clave del proyecto}

\subsection{Dockerfile}

\begin{lstlisting}[caption={Dockerfile}]
FROM python:3.12-slim AS runtime

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PIP_NO_CACHE_DIR=1

WORKDIR /app

RUN apt-get update \
 && apt-get install -y --no-install-recommends curl \
 && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

RUN chmod +x /app/entrypoint.sh \
 && mkdir -p /app/staticfiles

EXPOSE 8000

ENTRYPOINT ["/app/entrypoint.sh"]
CMD ["gunicorn", "core.wsgi:application", "--bind", "0.0.0.0:8000", "--workers", "3", "--access-logfile", "-"]
\end{lstlisting}

Se utiliza la imagen oficial \texttt{python:3.12-slim} como base, por su balance entre tamaño y ergonomía. El \texttt{ENTRYPOINT} se encarga de preparar la base de datos antes de ejecutar el comando principal.

\subsection{compose.yml}

\begin{lstlisting}[caption={compose.yml}]
services:
  db:
    image: docker.io/library/postgres:16-alpine
    container_name: clinica_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 10
    networks:
      - clinica_net

  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: clinica-ioo:latest
    container_name: clinica_web
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    networks:
      - clinica_net

volumes:
  pgdata:

networks:
  clinica_net:
\end{lstlisting}

\subsection{entrypoint.sh}

El \texttt{entrypoint} se ejecuta cada vez que arranca el contenedor web. Realiza cuatro pasos secuenciales:

\begin{enumerate}
    \item \textbf{Espera activa a PostgreSQL}: intenta una conexión con \texttt{psycopg2} cada segundo, hasta 60 intentos.
    \item \textbf{Migraciones}: ejecuta \texttt{python manage.py migrate --noinput} para aplicar el esquema.
    \item \textbf{Archivos estáticos}: ejecuta \texttt{collectstatic} para que WhiteNoise pueda servirlos.
    \item \textbf{Superusuario}: si están definidas las variables \texttt{DJANGO\_SUPERUSER\_USERNAME} y \texttt{DJANGO\_SUPERUSER\_PASSWORD}, crea o actualiza el superusuario de administración.
\end{enumerate}

Finalmente ejecuta el \texttt{CMD} del Dockerfile (gunicorn) con \texttt{exec "\$@"}, reemplazando el proceso del shell.

\subsection{Variables de entorno}

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.3}
\begin{tabularx}{\textwidth}{|>{\ttfamily\small}p{5.3cm}|X|}
\hline
\rowcolor[RGB]{25,55,109}
\textcolor{white}{\textbf{Variable}} & \textcolor{white}{\textbf{Descripción}} \\
\hline
SECRET\_KEY & Clave de firma de Django. \\
\hline
DEBUG & Modo depuración (True/False). \\
\hline
ALLOWED\_HOSTS & Lista separada por comas de hosts permitidos. \\
\hline
POSTGRES\_DB & Nombre de la base. \\
\hline
POSTGRES\_USER & Usuario de la base. \\
\hline
POSTGRES\_PASSWORD & Contraseña de la base. \\
\hline
DATABASE\_URL & URL completa leída por \texttt{django-environ}. \\
\hline
DJANGO\_SUPERUSER\_USERNAME & Usuario administrador creado automáticamente. \\
\hline
DJANGO\_SUPERUSER\_EMAIL & Correo del administrador. \\
\hline
DJANGO\_SUPERUSER\_PASSWORD & Contraseña del administrador. \\
\hline
\end{tabularx}
\caption{Variables de entorno del proyecto}
\end{table}

% ============================================================================
% 5. PROCESO DE DESPLIEGUE
% ============================================================================
\section{Proceso de despliegue}

\subsection{Requisitos previos}

Sobre una distribución Linux (CachyOS / Arch en el caso de este equipo), instalar:

\begin{lstlisting}[language=bash]
sudo pacman -S podman podman-compose
\end{lstlisting}

\subsection{Pasos de despliegue}

\begin{enumerate}
    \item Clonar el repositorio:
    \begin{lstlisting}[language=bash]
git clone https://github.com/Chatesito/trabajo-ioo.git
cd trabajo-ioo
    \end{lstlisting}

    \item Crear el archivo \texttt{.env} a partir de la plantilla:
    \begin{lstlisting}[language=bash]
cp .env.example .env
    \end{lstlisting}

    \item Levantar los contenedores:
    \begin{lstlisting}[language=bash]
podman-compose up --build -d
    \end{lstlisting}

    \item Verificar:
    \begin{lstlisting}[language=bash]
podman ps
curl http://localhost:8000/pacientes/
    \end{lstlisting}
\end{enumerate}

Para detener los servicios al finalizar la demostración basta con \texttt{podman-compose down} (sin el flag \texttt{-v}, que conservaría los datos del volumen).

\subsection{Evidencias del despliegue}

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{imagenes/03-build-up.png}
\caption{Salida de \texttt{podman-compose up --build -d}: construcción de la imagen y arranque de los servicios.}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{imagenes/04-containers-running.png}
\caption{Contenedores en ejecución (\texttt{podman ps}). Obsérvese el estado \texttt{(healthy)} en \texttt{clinica\_db}.}
\end{figure}

\subsection{Credenciales del administrador (entorno de demostración)}

\textit{Nota: las credenciales que se muestran a continuación corresponden a un entorno de demostración aislado. No deben utilizarse en ningún despliegue de producción.}

El \texttt{entrypoint.sh} crea automáticamente el siguiente superusuario en el admin de Django a partir de variables de entorno:

\begin{itemize}
    \item \textbf{URL:} \url{http://localhost:8000/admin/}
    \item \textbf{Usuario:} \texttt{Leon}
    \item \textbf{Contraseña:} \texttt{changeme}
\end{itemize}

\begin{figure}[H]
\centering
\includegraphics[width=0.82\textwidth]{imagenes/12-admin-django.png}
\caption{Panel de administración de Django con las entidades \texttt{Paciente} y \texttt{Cita} registradas.}
\end{figure}

% ============================================================================
% 6. PERSISTENCIA
% ============================================================================
\section{Verificación de la persistencia}

Para demostrar que los datos no se pierden entre ejecuciones se siguió este procedimiento:

\begin{enumerate}
    \item Se levantaron los contenedores con \texttt{podman-compose up -d}.
    \item Se crearon varios pacientes y citas desde la interfaz web.
    \item Se detuvieron los contenedores con \texttt{podman-compose down} (sin el flag \texttt{-v}).
    \item Se volvieron a levantar con \texttt{podman-compose up -d}.
    \item Se comprobó que todos los pacientes y citas seguían disponibles.
\end{enumerate}

Esto es posible gracias al volumen nombrado \texttt{pgdata}, que Podman reconoce y reutiliza en todos los arranques posteriores al primero.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{imagenes/05-volumen-creado.png}
\caption{Volumen \texttt{pgdata} listado por Podman (\texttt{podman volume ls}). Sobrevive a \texttt{podman-compose down}.}
\end{figure}

% ============================================================================
% 7. PUBLICACIÓN EN DOCKER HUB
% ============================================================================
\section{Publicación en Docker Hub}

Para esta entrega se utilizan dos cuentas distintas, separadas por servicio:

\begin{itemize}
    \item \textbf{Repositorio de código} (GitHub): cuenta personal de Juan David Rivera (\texttt{Chatesito}) --- \url{https://github.com/Chatesito/trabajo-ioo}.
    \item \textbf{Imagen del contenedor} (Docker Hub): cuenta personal de Jorge León Chavarro (\texttt{donleonz}) --- \url{https://hub.docker.com/r/donleonz/clinica-ioo}.
\end{itemize}

\subsection{Autenticación}

Desde Podman:

\begin{lstlisting}[language=bash]
podman login docker.io
\end{lstlisting}

\subsection{Etiquetado}

\begin{lstlisting}[language=bash]
podman tag clinica-ioo:latest docker.io/donleonz/clinica-ioo:latest
podman tag clinica-ioo:latest docker.io/donleonz/clinica-ioo:v1.0
\end{lstlisting}

\subsection{Publicación}

\begin{lstlisting}[language=bash]
podman push docker.io/donleonz/clinica-ioo:latest
podman push docker.io/donleonz/clinica-ioo:v1.0
\end{lstlisting}

La imagen queda públicamente disponible en Docker Hub y cualquier persona puede descargarla y ejecutarla con:

\begin{lstlisting}[language=bash]
podman pull docker.io/donleonz/clinica-ioo:latest
\end{lstlisting}

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{imagenes/13-dockerhub-web.png}
\caption{Repositorio público de la imagen en Docker Hub: \texttt{donleonz/clinica-ioo}.}
\end{figure}

% ============================================================================
% 8. CONCLUSIONES
% ============================================================================
\section{Conclusiones}

A continuación se evalúa el cumplimiento de los objetivos planteados al inicio del informe y se recogen las lecciones técnicas más relevantes del proceso.

\subsection{Cumplimiento de objetivos}

\begin{itemize}
    \item \textbf{CRUD orientado a objetos con dos entidades}: cumplido. Las clases \texttt{Paciente} y \texttt{Cita} modelan el dominio mediante el ORM de Django, con validadores encapsulados, relación 1 a N por \texttt{ForeignKey} y vistas basadas en clases (\textit{Class-Based Views}) que reutilizan jerarquías de Django.
    \item \textbf{Arquitectura multi-contenedor con separación de responsabilidades}: cumplido. El servicio \texttt{web} (Django + gunicorn) y el servicio \texttt{db} (PostgreSQL 16) viven en contenedores independientes, conectados por una red privada y con un único puerto expuesto al host.
    \item \textbf{Persistencia mediante volúmenes nombrados}: cumplido y verificado experimentalmente. El volumen \texttt{pgdata} sobrevive a \texttt{podman-compose down}, reinicios del equipo y reconstrucciones de la imagen del servicio web.
    \item \textbf{Automatización del arranque}: cumplido. \texttt{podman-compose} orquesta en un solo comando la construcción de la imagen, la creación de la red privada y el volumen, el arranque ordenado de los contenedores (con \textit{healthchecks}) y la ejecución de migraciones, recolección de estáticos y creación del superusuario desde el \texttt{entrypoint}.
    \item \textbf{Publicación en Docker Hub}: cumplido. La imagen está disponible públicamente con dos tags (\texttt{latest} y \texttt{v1.0}) y puede ejecutarse indistintamente con Podman o Docker.
\end{itemize}

\subsection{Lecciones técnicas}

\begin{itemize}
    \item Los contenedores son \textbf{efímeros por diseño}; los volúmenes son la única forma correcta de garantizar la durabilidad de los datos en un servicio con estado como una base de datos.
    \item Una arquitectura de dos contenedores con \textbf{red privada} y un único puerto expuesto reproduce el principio de menor privilegio: la base de datos nunca es accesible desde el exterior, sólo desde el servicio que la consume.
    \item La espera activa del \texttt{entrypoint} sobre la base de datos resultó imprescindible: \texttt{podman-compose} no siempre respeta la condición \texttt{service\_healthy}, y la verificación adicional evitó condiciones de carrera durante el primer arranque.
    \item Podman se confirmó como un reemplazo directo de Docker en Linux. Al respetar el formato OCI, la imagen publicada se ejecuta indistintamente con \texttt{podman} o \texttt{docker} sin modificación alguna.
    \item El paradigma orientado a objetos, aplicado mediante el ORM, hace explícitas las invariantes del dominio (validadores en las clases, integridad referencial en las relaciones) sin depender de la capa de formularios.
\end{itemize}

% ============================================================================
% 9. REFERENCIAS
% ============================================================================
\section{Referencias}

\begin{itemize}[leftmargin=*, labelsep=0.5em]
    \item Django Software Foundation. (2025). \textit{Django documentation} (versión 6.0). Recuperado de \url{https://docs.djangoproject.com/en/6.0/}
    \item Red Hat, Inc. (2025). \textit{Podman documentation}. Recuperado de \url{https://docs.podman.io/}
    \item The PostgreSQL Global Development Group. (2025). \textit{PostgreSQL official image}. Docker Hub. Recuperado de \url{https://hub.docker.com/_/postgres}
    \item Whitenoise contributors. (2025). \textit{WhiteNoise documentation}. Recuperado de \url{https://whitenoise.readthedocs.io/}
    \item Open Container Initiative. (2024). \textit{OCI image and runtime specifications}. Recuperado de \url{https://opencontainers.org/}
    \item \textit{Tutorial de Docker — referencia audiovisual sugerida en la guía del curso} [Video]. YouTube. Recuperado de \url{https://www.youtube.com/watch?v=6wbfODetPbE}
    \item \textit{Tutorial de Docker Compose — referencia audiovisual sugerida en la guía del curso} [Video]. YouTube. Recuperado de \url{https://www.youtube.com/watch?v=CV_Uf3Dq-EU}
    \item Repositorio del proyecto: Rivera Chaté, J. D. (2026). \textit{trabajo-ioo} [Código fuente]. GitHub. \url{https://github.com/Chatesito/trabajo-ioo}
\end{itemize}

\end{document}
